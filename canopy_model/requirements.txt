# Yam canopy U-Net -- pinned to the environment this pilot was trained and
# validated in (see docs/ for CV results). Install with:
#   pip install -r requirements.txt
#
# NOTE: exact versions were not captured from the original "yam-fcover" conda
# env before this handoff. The ranges below are safe defaults for this stack.
# Whoever runs train_unet_5fold.py or predict_unet.py first should run
# `pip freeze > requirements.lock.txt` in their working env and commit that
# lock file, so the pinned pilot results stay reproducible going forward.

torch>=2.0,<3.0
torchvision>=0.15,<1.0
segmentation-models-pytorch>=0.3,<0.4
albumentations>=1.3,<2.0
rasterio>=1.3,<2.0
Pillow>=9.0
numpy>=1.24,<2.0
