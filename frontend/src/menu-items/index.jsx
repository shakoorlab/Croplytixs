// project import
import trials from './trials';
import models from './models';

// ==============================|| MENU ITEMS ||============================== //

/**
 * The drawer's groups, in order. Each group's routes live in routes/MainRoutes.
 */
const menuItems = {
  items: [trials, models]
};

export default menuItems;
