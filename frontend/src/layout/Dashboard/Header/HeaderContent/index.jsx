// project imports
import Profile from './Profile';

// ==============================|| HEADER - CONTENT ||============================== //

/**
 * Right side of the app bar. Only the account menu for now — the template's search box
 * and notification bell were placeholders with nothing behind them. Anything added here
 * should do something.
 */
export default function HeaderContent() {
  return <Profile />;
}
